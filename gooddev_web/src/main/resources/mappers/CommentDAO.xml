<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.good.gooddev_web.board.dao.CommentDAO">

	<select id="getList" resultType="io.good.gooddev_web.board.vo.CommentVO">
		select *
		from good_comment
		<where>
			<if test="bno != null and bno.length >0">
				bno = #{bno}
			</if>
			<if test="cno != null and cno.length >0">
				<trim prefix="and"> parent_cno = #{cno} </trim>
			</if>
		</where>
	</select>

	
	<insert	id="insert">
		insert into good_comment (cno, mid, comment_content, bno) values (cno_sequence.NEXTVAL, #{mid, jdbcType=VARCHAR}, #{comment_content, jdbcType=VARCHAR}, #{bno, jdbcType=NUMERIC})
	</insert>

</mapper>
